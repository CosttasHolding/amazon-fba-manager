---
fecha: 2026-08-03
tipo: daily-note
tags: [diario, desarrollo]
---

# 2026-08-03 - Score enriquecido + source_data visible en cards kanban

## Que se hizo
- [commit] Commiteo y pusheo de los fixes de extension pendientes (10ma-13va parte): `c0257b4` (reader AMZScout tabla+totals, deteccion `amzscout-pro`, observer con fingerprint, fix tabla del nicho) + vault
- [spec] Design doc del research: mostrar source_data + score enriquecido en la UI — `docs/superpowers/specs/2026-08-02-research-score-source-data-ui-design.md`
- [plan] Plan de implementacion TDD 4 tareas — `docs/superpowers/plans/2026-08-02-research-score-source-data-ui.md`
- [feature] **Columna `score` en `product_research`** (migracion `030_add_score.sql`) + campo `score?: number | null` en `ProductResearch`
- [feature] **Capture route calcula y persiste el score**: `POST /api/research/capture` arma el `ScoringInput` desde el source_data completo (sales, revenue, bsr, reviews, rating, sellers FBA, price, fees) y llama `calculateScore()`; guarda `score` en la columna + `score_details` (4 dimensiones) en `source_data`; el UPDATE de ASIN existente refresca el score
- [feature] **Cards kanban muestran los datos capturados**: badge de Score (coloreado por rango) + badges de BSR, ventas/m, revenue/m, margen %, listing health — cada uno solo si el dato existe
- [feature] Helpers puros nuevos `src/lib/research/card-data.ts` (`numField`, `fmtCompact`) con tests
- [i18n] Keys `research.card.*` en es/en/ar (BSR, Health, Margen, /m, ventas/m, Score)
- [verify] Verificacion completa: tsc 0 errores | lint solo warnings pre-existentes | tests **255/255** (33 archivos) | build OK
- [review] SDD por tareas (4 implementers + 4 reviewers + final whole-branch review): **READY TO MERGE**
- [push] 7 commits a `origin/main` (deploy a Vercel)
- [verify] **Usuario aplico la migracion `030_add_score.sql` en Supabase prod** (columna `score` confirmada)

## Decisiones
- **Enfoque A de persistencia**: columna `score` dedicada (ordenable/filtrable desde DB) + `score_details` en el JSONB — en vez de solo meterlo en source_data
- **Calcular al capturar** (no en frontend): el score queda persistido y la card lo muestra sin recalculo
- **`score` null si no hay datos** (gate `hasData`), no un 0 fake — distingue "sin datos" de "datos con score bajo"
- **Scoring inmutable**: `calculateScore` solo se consume, nunca se modifica
- **BSR**: columna primero, fallback a `source_data.bsr`

## Archivos modificados
- NUEVO: `supabase/migrations/030_add_score.sql` — columna score INTEGER
- NUEVO: `src/lib/research/card-data.ts` + `card-data.test.ts` — numField/fmtCompact
- NUEVO: `docs/superpowers/specs/2026-08-02-research-score-source-data-ui-design.md`
- NUEVO: `docs/superpowers/plans/2026-08-02-research-score-source-data-ui.md`
- `src/types/index.ts` — campo `score` en ProductResearch
- `src/app/api/research/capture/route.ts` — calculo de score + score_details
- `src/app/api/research/capture/route.test.ts` — +3 tests (score>0, null, refresh en UPDATE)
- `src/app/(dashboard)/research/page.tsx` — badges en card kanban
- `src/lib/i18n/{es,en,ar}.json` — 6 keys `research.card.*`
- Vault: `App State.md`, `Bugs Conocidos.md`, `PROMPT_NEXT_SESSION.md`, `Learning Log.md`, `Daily Notes/2026-08-03.md`

## Proximos pasos
- [ ] **USUARIO: verificar extension con AMZScout logueado** (Reload + F5 + abrir producto → 1 solo producto con Ventas/m + Revenue/m + Margen)
- [ ] **USUARIO: cargar creditos/licencias xAI** (deep dive tira 403) + **agregar XAI_API_KEY en Vercel** + **rotar keys**
- [ ] Follow-up (MEDIUM): score queda obsoleto si se editan ventas/precio manualmente (PUT /api/research no recalcula) — recalcular en PUT o documentar como snapshot de captura
- [ ] Backlog: Zod en SP-API/Drive/Cron, N+1, accessibility
- [ ] Deuda i18n: `product-analyzer.tsx` strings hardcodeadas (pre-existente)
- [ ] Unificar numeros duplicados de migraciones 014/015 (LOW)

## Notas
- La sesion cruzo la medianoche: spec/plan fechados 08-02 (cuando se escribieron), daily note 08-03 (fecha real)
- 3 desvios del brief del Task 4 que eran correcciones correctas: (1) test data `91992` → "92K" por ICU rounding, se cambio a `91900`; (2) `sourceBadges` locale tipado `Locale` (t() lo exige); (3) guard `!= null` porque `score` es opcional (undefined posible en filas pre-migracion)
- Finding del final review que queda como follow-up: el score es un snapshot de captura; ediciones manuales via modal no lo recalculan

---

# 2026-08-03 (2da sesion) - Nicho + competencia en 5 niveles

## Que se hizo
- [feature] **`CompetitionLevel` de 3 → 5 valores** (`very_low`/`low`/`medium`/`high`/`very_high`): type en `@/types`, zod enum en `research.ts`, migracion `031_competition_5_levels.sql`
- [feature] **Helper puro `competitionLevelFromScore`** en `src/lib/research/competition.ts` (thresholds >=80/60/40/20) con 5 tests de boundary
- [feature] **Capture route completa `niche`** (categoria) y **deriva `competition_level`** desde `competenciaScore` (2 tests nuevos)
- [i18n] Keys `research.competition.very_low/very_high` en es/en/ar
- [feature] **UI**: modal con 5 opciones de competencia + badge kanban traducido (sin `capitalize` raw)
- [feature] **Extension parsea `niche_score`** de los totals de AMZScout (totals-item "Niche Score")
- [feature] **Popup deriva y muestra competencia** localmente: `popup/competition.ts` (`competitionLevelFromCaptured`, port de `competenciaScore`) + badge "Competencia:" con labels; fallback a `p.competition_level`
- [verify] Verificacion completa: tsc 0 | lint solo warnings pre-existentes | **268/268 tests** (35 archivos) | build OK | build:extension OK
- [sync] Copia personal `C:\Users\Nacho\Desktop\Amazon\IMPORTANTE\exteRB\` sincronizada

## Decisiones
- **Port de `competenciaScore` en el popup**: el popup se buildea aparte y no puede importar `@/lib`; se porto la formula a `popup/competition.ts` como espejo exacto (s=100, sellers*3.5 cap 75, log2(reviews+1)*3 cap 20, (5-rating)*5 cap 20, clamp 0-100)
- **`niche` = categoria**: en capture, `niche: p.category ?? null` (nullable) al lado de `amazon_category ?? ""` (default vacio)
- **Migracion 031**: drop + re-add del CHECK constraint (no destructivo, IF EXISTS)
- **`public/exteRB` NO se commitea**: esta gitignored (build output local); los briefs lo incluian por error pero el repo no lo trackea

## Archivos modificados
- `src/lib/research/competition.ts` + `competition.test.ts` — helper nuevo (Task 1)
- `src/types/index.ts`, `src/validations/research.ts`, `src/validations/schemas.test.ts` — CompetitionLevel 5 valores (Task 2)
- `supabase/migrations/031_competition_5_levels.sql` — nueva migracion (Task 2)
- `src/app/api/research/capture/route.ts` + test — niche + competition_level (Task 3)
- `src/lib/i18n/{es,en,ar}.json` — 2 keys cada uno (Task 4)
- `src/app/(dashboard)/research/page.tsx` — modal + badge kanban (Task 5)
- `src/chrome-extension/content/overlay-reader.ts` + test — niche_score (Task 6)
- `src/chrome-extension/popup/competition.ts` + test + `popup.ts` — competencia local (Task 7)
- Vault: `App State.md`, `Bugs Conocidos.md`, `PROMPT_NEXT_SESSION.md`

## Proximos pasos
- [ ] **USUARIO: aplicar migracion `031_competition_5_levels.sql` en Supabase prod**
- [ ] **USUARIO: verificar extension con AMZScout** (Reload + F5 + producto → popup con Competencia + card kanban con badge)
- [ ] **USUARIO: cargar creditos/licencias xAI** + **agregar XAI_API_KEY en Vercel** + **rotar keys**
- [ ] Follow-up (MEDIUM): score/competition_level stale si se editan datos manualmente (PUT no recalcula) — aplica tambien a competition_level
- [ ] Push a origin/main (deploy a Vercel)

## Notas
- Tasks 1-6 con implementer+reviewer subagentes (todos clean); Task 2 necesito 2 dispatch (el 1ro quedo a medio hacer y escribio un reporte viejo encima), Task 7-8 directo con verificacion
- Commits: `24fce3e`, `bd23c16`, `e04c183`, `176b86c`, `85bfcd1`, `2cc76b1`, `bd3d511`
