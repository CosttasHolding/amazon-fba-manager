---
fecha: 2026-08-01
tipo: daily-note
tags: [diario, desarrollo]
---

# 2026-08-01 - Verificacion prod, minor findings y tsc a cero errores

## Que se hizo
- [verify] Migration `source_data` YA estaba aplicada en Supabase prod (verificado via PostgREST — el vault estaba desactualizado)
- [verify] Test E2E server-side del motor: probe insert/delete en prod con el payload exacto del capture endpoint — schema 100% compatible
- [verify] OPENAI_API_KEY agregada a `.env.local` (usuario la proveyo) — key VALIDA pero la cuenta NO tiene creditos (429 billing). Deep dive falla hasta cargar creditos
- [fix] Fallback enums GPT: Zod schema con `.catch()` por campo en `analyzer.ts` (market_fit→medium, difficulty→moderate, arrays→[], strings→"") — 6 tests nuevos
- [fix] Deep dive accesible desde vista kanban (boton Sparkles en cards, antes solo vista lista)
- [fix] i18n en componentes nuevos: 27 keys `research.deepdive.*`/`research.analyzer.*`/`research.extension.*` en es/en/ar; deep-dive-panel y research page migrados a `t()`
- [fix] 41 errores tsc pre-existentes eliminados: `createMockRequest` ahora retorna `NextRequest` (cast interno), fix cast `as unknown as` en sp-api client.test, anotacion de retorno en suppliers buildQueryChain — **tsc: 0 errores**
- [fix] `build-extension.ts` cross-platform: PowerShell Compress-Archive → `adm-zip` (Node puro); zip regenerado y verificado (estructura + manifest + host_permissions OK)
- [fix] `tsx` y `esbuild` agregados a devDependencies (el script los usaba via npx sin declararlos — bug latente en npm ci)
- [refactor] Migration renombrada `20260730_add_source_data.sql` → `029_add_source_data.sql` (convencion NNN_ del repo)

## Decisiones
- Fallbacks Zod con `.catch()` por campo (no `.parse()` estricto): un solo campo malo de GPT no tira todo el analisis
- Fix de los 41 errores centralizado en `mock-request.ts` (return type `NextRequest`) en vez de editar 40 call sites — cero cambios de logica en tests
- `adm-zip` como devDependency para zips cross-platform (Windows-only era el finding)
- NO se toco `product-analyzer.tsx` (strings hardcodeadas) — es pre-existente (Research Bot 0272435), fuera del scope del finding "componentes nuevos". Queda como deuda i18n registrada

## Archivos modificados
- `src/lib/research/analyzer.ts` — Zod schema con fallbacks
- NUEVO: `src/lib/research/analyzer.test.ts` — 6 tests
- `src/components/research/deep-dive-panel.tsx` — i18n completo
- `src/app/(dashboard)/research/page.tsx` — i18n + boton deep dive en kanban
- `src/lib/i18n/{es,en,ar}.json` — 27 keys nuevas por locale
- `src/lib/test-utils/mock-request.ts` — return type NextRequest
- `src/lib/sp-api/client.test.ts` — cast via unknown
- `src/app/api/suppliers/route.test.ts` — return type explicito en buildQueryChain
- `src/scripts/build-extension.ts` — adm-zip en vez de PowerShell
- `package.json` — devDeps: adm-zip, @types/adm-zip, tsx, esbuild
- `supabase/migrations/029_add_source_data.sql` — renombrada
- `.env.local` — OPENAI_API_KEY (gitignored)

## Build
- tsc: **0 errores** (antes 41 pre-existentes)
- Tests: **217/217** (211 + 6 nuevos)
- Lint: solo warnings pre-existentes (`<img>` en auth)
- Build prod: OK
- Extension zip regenerado y verificado

## Grok (xAI) — migracion de proveedor LLM
- [feature] Deep dive y analisis de productos migrados de **OpenAI GPT-4o a xAI Grok** (usuario proveyo key `XAI_API_KEY`)
- `getOpenAI` renombrado a `getXAIClient` en `src/lib/ai/client.ts` — mismo SDK de OpenAI, `baseURL: "https://api.x.ai/v1"`, modelo **`grok-4.5`** (flagship actual; `grok-3` retirado en mayo 2026)
- Actualizados consumidores: `analyzer.ts` (deep dive) y `analyze/route.ts` (Research Bot)
- Actualizados mocks en tests (`analyzer.test.ts`, `analyze-deep/route.test.ts`)
- `OPENAI_API_KEY` eliminada de `.env.local` (muerta y comprometida)
- `.env.example` documenta `XAI_API_KEY`
- Verificado con llamada real: la key autentica OK y llega a xAI, pero el **team no tiene creditos/licencias** (HTTP 403) — cargar en https://console.x.ai/team/db62d709-49a7-4db0-a4cd-d58a3921a13c

## Proximos pasos
- [ ] **USUARIO: cargar creditos/licencias en xAI** — deep dive tira 403 sin creditos (https://console.x.ai/team/db62d709-49a7-4db0-a4cd-d58a3921a13c)
- [ ] **USUARIO: agregar XAI_API_KEY en Vercel** (prod) — solo esta en .env.local
- [ ] **USUARIO: rotar API keys AL CARGAR CREDITOS** (decision: riesgo bajo hoy, sin creditos) — xAI: generar key nueva en console.x.ai; OpenAI: ya se dejo de usar
- [ ] **USUARIO: diagnostico extension Chrome** — reportar que muestra chrome://extensions
- [ ] Post-creditos: probar deep dive real con Grok (verificar `response_format` json_object y que el modelo responda JSON valido)
- [ ] Backlog: Zod en SP-API/Drive/Cron, N+1, accessibility
- [ ] Deuda i18n: `product-analyzer.tsx` tiene strings hardcodeadas (pre-existente)
- [ ] Unificar numeros duplicados de migraciones 014/015 (LOW)

## Seguridad (hardening post-exposicion de keys)
- [sec] Usuario expuso 2 API keys en el chat (OpenAI + xAI) — auditoria: LIMPIO en repo (git grep, historial, filesystem). La exposicion real es el transcript del chat
- [sec] **Pre-commit hook anti-secretos**: Husky + `scripts/check-secrets.js` — bloquea commits con patrones (sk-proj-, xai-, AKIA, AIza, Stripe, GitHub, Slack, PRIVATE KEY, JWT). Probado: bloquea key falsa (exit 1) y deja pasar archivo normal (exit 0)
- [sec] `.gitignore` reforzado: `.env.*` (excepto `.env.example`), `*.pem`, `*.key`, `*.p8`, `*.p12`, `*.pfx`, `*.pkcs8`, service-account json, credentials json. Se elimino el fragmento corrupto "esto es"
- [sec] AGENTS.md: regla "Seguridad > todo" — keys solo en `.env.local`, nunca en repo/chat; no usar `--no-verify`
- [verify] Barrido de todos los archivos trackeados por 9 patrones de secretos: LIMPIO
- **PENDIENTE USUARIO**: rotar ambas keys (xAI: console.x.ai, OpenAI: platform.openai.com) y pasar las nuevas

## Notas
- El analyze-deep route devuelve `error.message` crudo del LLM — util ahora (muestra el 403 de creditos) pero revisar sanitizacion si se endurece seguridad
- El zip de la extension se regenero con adm-zip — estructura identica a la verificada en Playwright

---

# 2026-08-01 (2da parte) — Diagnostico y fix de la extension Chrome

## Que se hizo
- [diagnose] **Bug "la extension nunca me funciono" — CAUSA RAZ CONFIRMADA**: el deploy de produccion es VIEJO y no incluia el zip. `public/research/extension.zip` (trackeado, VALIDO en HEAD) no se servia en Vercel
- [verify] `/LOGO.png` → 200 directo (Vercel sirve `public/` sin auth); `/research/extension.zip` → **307 a /login** (el archivo no existe en el deploy → el usuario descargaba la pagina HTML como "zip" → Chrome la rechazaba)
- [fix] Zip movido a **`public/extension.zip`** (raiz, sin colision con el segmento de ruta `/research`) — commit `91df13f`
- [fix] Link de descarga en `research/page.tsx` → `/extension.zip`; `build-extension.ts` ahora genera `public/extension.zip`
- [cleanup] Eliminados restos sin trackear de `public/research/` (manifest.json, popup/, content/) y el fragmento corrupto `esto es`
- [commit] `91df13f` (fix extension) + `dd67f31` (consolidacion sesion) pusheados a main → Vercel redeployo

## Decisiones
- Ruta raiz `/extension.zip` en vez de API route: mas simple y elimina el riesgo de shadowing por la ruta `/research` (segmento de dashboard)
- El zip se sirve como estatico; el middleware corre antes (matcher no excluye `.zip`) pero un usuario logueado pasa y recibe el archivo

## Proximos pasos
- [ ] **USUARIO: instalar la extension** — Load unpacked sobre `public/exteRB/` (verificada OK en Chromium)
- [ ] **USUARIO: mover `public/exteRB.pem` fuera del repo** (clave privada de firma — gitignored, no commiteada, pero no debe quedar en public/)
- [ ] **TESTEAR envio (follow-up)**: el fetch del popup a `/api/research/capture` con `credentials: "include"` y cookie `SameSite=Lax` puede dar 401 — confirmar con la extension instalada y logueado

---

# 2026-08-01 (3ra parte) — Extension como carpeta exteRB (sin zip)

## Que se hizo
- [refactor] Extension distribuida como **carpeta `public/exteRB/`** (manifest.json + content/ + popup/) en vez de zip — por decision del usuario (Load unpacked directo)
- [cleanup] `public/extension.zip` eliminado; `public/extension-dist/` → renombrada a `public/exteRB/`
- [fix] `build-extension.ts`: DIST → `public/exteRB`, sin paso de zip (adm-zip ya no se usa)
- [fix] `.gitignore`: `public/extension-dist/` → `public/exteRB/`
- [fix] Seccion de descarga eliminada de `research/page.tsx` (ya no hay zip) + keys i18n `research.extension.*` removidas de es/en/ar
- [verify] **Carga de extension verificada en Chromium/Playwright**: enabled, sin errores, "FBA Research Agent" (ID asignado)
- [sec] El usuario genero `exteRB.crx` + `exteRB.pem` (Pack extension) — `.pem` es clave privada: gitignored, no commiteada, PENDIENTE moverla fuera de public/

## Verificacion
- tsc: 0 errores | lint: solo warnings pre-existentes | tests: 217/217 | build: OK
- Extension exteRB carga en Chromium: PASS

---

# 2026-08-01 (4ta parte) — Recolector multi-fuente (H10/AMZScout/Keepa) + fix scraper

## Que se hizo
- [bug] **"No me toma datos, nunca pone nicho"** — diagnostico confirmado con Playwright vs amazon.com:
  - El scraper de resultados captura 44 cards pero con bugs: el titulo tomaba "Patrocinado/Estas viendo este anuncio..." (selector `h2 a, h2 span, [class*='title']` con match equivocado), `[data-asin]` matcheaba cards ANIDADAS → duplicados, y el precio llegaba en ARS con `currency` fijo USD
  - **Limitacion estructural**: BSR, categoria/nicho y ventas NO estan en el DOM de busqueda de Amazon — solo vienen del overlay Xray completo (H10) o de la pagina del producto. Con H10 free, `detectH10Xray()` NO detecta el overlay
  - En la app, `niche`/`amazon_category` los llena solo el LLM (deep dive, bloqueado por creditos xAI) o Research Bot — el scraper no puede producirlos
- [decide] Plan multi-fuente: la extension pasa a ser un **recolector que lee overlays de H10 free + AMZScout + Keepa** (usuario ya instalo AMZScout y Keepa free) — esas son las fuentes reales de BSR/ventas/nicho
- [feature] NUEVO `content/overlay-reader.ts` — `readOverlay(container)`: lector generico por ASIN (fila/`[data-asin]`/link /dp/) con parseo de numeros localizados y deteccion de moneda; campos BSR, sales, revenue, FBA fee, sellers FBA/FBM, niche score, brand, category
- [feature] NUEVO `content/sources.ts` — `detectOverlays()` (H10/AMZScout/Keepa) + `collectOverlayDebugHtml()` para el tool de debug
- [refactor] `content/content.ts` reescrito: scrape base (search o product) → merge por ASIN con overlays (prioridad h10 > amzscout > keepa) → campos extra al `source_data`; MutationObserver re-dispara al detectar overlay nuevo; responde `GET_OVERLAY_DEBUG`
- [fix] `content/scraper.ts`: titulo real (`h2 a span, h2 a, .a-link-normal.s-link-style`), dedupe de `[data-asin]` anidados (`outermostAsinCards`), moneda detectada (ARS/EUR/GBP/CAD/MXN por simbolo o hostname, no fijo USD)
- [feature] Tool de debug en popup: boton "Debug overlays" → `GET_OVERLAY_DEBUG` → textarea con el HTML de los overlays detectados + boton copiar (para afinar selectores)
- [cleanup] `h10-reader.ts` y `detect-h10.ts` eliminados (reemplazados por overlay-reader/sources)
- [build] `npm run build:extension` → exteRB regenerado OK (content.js incluye la logica multi-fuente)

## Verificacion
- tsc: 0 errores | lint: solo warnings pre-existentes | tests: 217/217 | build extension: OK

## Proximos pasos
- [ ] **USUARIO: recargar la extension en chrome://extensions** (Load unpacked → `public/exteRB/`)
- [ ] **USUARIO: abrir una busqueda de Amazon con AMZScout/Keepa activos, y correr "Debug overlays" en el popup** → pegar el HTML de los overlays para afinar selectores
- [ ] Con el DOM real: afinar readers de AMZScout (niche score, ventas, revenue) y Keepa (BSR), mapear al capture endpoint y a `source_data` JSONB
- [ ] Test Playwright del scraper arreglado (titulo/duplicados/moneda) en amazon.com
- [ ] Verificar envio: fetch del popup a `/api/research/capture` con `credentials: "include"` (posible 401 por cookie SameSite=Lax)
