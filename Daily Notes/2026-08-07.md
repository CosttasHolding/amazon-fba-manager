---
fecha: 2026-08-07
tipo: daily-note
tags: [diario, desarrollo]
---

# 2026-08-07 - Verificacion prod + investigacion niche_score

## Que se hizo
- [verify] **Verificacion de los 4 pendientes del usuario contra prod** (solo lectura via PostgREST con service role key, sin exponer secrets):
  - Migracion `033_research_urls.sql` → **APLICADA** (columnas `amazon_url`/`alibaba_url` existen; 2 filas con amazon_url)
  - Migracion `032_research_metrics.sql` → **APLICADA** (columnas `estimated_monthly_revenue`/`estimated_fba_fee`/`seller_count_fba` existen)
  - Migracion `031_competition_5_levels.sql` → aplicada (ya estaba confirmada en App State)
  - E2E extension → **VERIFICADO en datos**: 2 capturas AMZScout reales del 2026-08-05 (`B016NE9A2A` Foam Roller score 85 comp=low mo_sales 5840; `B0H38PWZKR` Jujutsu Kaisen score 86 comp=low mo_sales 2041), ambas con amazon_url auto
  - Rotacion de keys → **confirmada por el usuario** (2026-08-05), no verificable tecnicamente
  - Creditos xAI → **SIGUE PENDIENTE** (deep dive bloqueado, 403)
- [investigacion] **Hallazgo [MEDIUM]: `niche_score` no se captura en pagina de producto**:
  - 0/68 filas con `source_data.niche_score`; las 3 capturas con score tienen niche_score null
  - **Causa raiz**: `readAMZScout` con `fallbackAsin` devuelve la fila del ASIN abierto desde la tabla del nicho (`readAmzscoutTable`, overlay-reader.ts:293), que **no parsea `niche_score`**; los totals del header (donde vive el total "Niche Score", leido por `readAmzscoutTotals`:288) solo se leen cuando el ASIN NO esta en la tabla y `Results <= 1` (overlay-reader.ts:334-344). En el caso real mas comun (producto logueado), la tabla SI tiene el ASIN → la ruta de totals no corre
  - El parser de totals SI funciona (test `AMZSCOUT_TOTALS_HTML` → 72 pasa); es un problema de ruta, no de parseo
  - **Fix propuesto**: mergear `niche_score` desde `readAmzscoutTotals` al match de la tabla. PENDIENTE decision
- [fix] **niche_score RESUELTO (TDD)**: `readAMZScout` con fallbackAsin ahora mergea `niche_score` desde los totals del header cuando el ASIN matchea en la tabla del nicho (overlay-reader.ts). Test RED primero (fixture `AMZSCOUT_TABLE_WITH_TOTALS_HTML`, esperaba 72 y recibia null), luego GREEN. Rebuild extension (`npm run build:extension`) + sync copia personal `exteRB` (content.js 13.9kb)
- [docs] Vault actualizado al estado real (App State, PROMPT_NEXT_SESSION, Bugs Conocidos, esta daily)

## Decisiones
- **Implementar el fix de niche_score (aprobado por el usuario con "go")**: merge de totals al match de la tabla, TDD. Alternativa descartada (parsear una columna de niche de la tabla) porque AMZScout no expone Niche Score por fila — vive solo en los totals del header
- **Verificacion via PostgREST en vez de la app**: lectura directa read-only, sin tocar datos; patron reutilizable para futuras verificaciones de migraciones
- **El matiz del Niche Score no bloquea nada**: el badge de competencia y el score derivan del scoring (funciona); el Niche Score de AMZScout es un dato complementario

## Archivos modificados
- `src/chrome-extension/content/overlay-reader.ts` — merge de `niche_score` desde totals al match de la tabla
- `src/chrome-extension/content/overlay-reader.test.ts` — fixture `AMZSCOUT_TABLE_WITH_TOTALS_HTML` + test RED (305 total)
- `App State.md` — estado real: migraciones 030-033 aplicadas, E2E verificado, fix niche_score
- `PROMPT_NEXT_SESSION.md` — pendientes del usuario reducidos a 1 (creditos xAI); fix niche_score DONE
- `Bugs Conocidos.md` — E2E movido a Resueltos; bug niche_score movido a Resueltos
- `Daily Notes/2026-08-07.md` — esta nota
- Sync externo: `C:\Users\Nacho\Desktop\Amazon\IMPORTANTE\exteRB\content\content.js` (13.9kb)

## Proximos pasos
- [ ] **USUARIO: cargar creditos xAI + XAI_API_KEY en Vercel** — unica pendiente de las 4
- [ ] **PUSH + deploy + E2E del fix niche_score**: capturar un producto nuevo con la extension actualizada y confirmar `source_data.niche_score` poblado
- [ ] Backlog (MEDIUM): Zod en SP-API/Drive/Cron, N+1, accessibility, deuda i18n product-analyzer, minor findings del review kanban

## Notas
- Las 4 pendientes del usuario eran: (1) migracion 033, (2) E2E extension, (3) creditos xAI, (4) rotar keys. Quedan 3 confirmadas (1,2,4) y 1 sin resolver (3).
- El usuario trabajo el 2026-08-05 fuera de sesion (capturas E2E); el vault no se habia actualizado desde el 08-04.
