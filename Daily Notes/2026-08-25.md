---
fecha: 2026-08-25
tipo: daily-note
tags: [diario, drive, seguridad, cierre]
---

# 2026-08-25 - Cierre de endurecimiento Drive multi-tenant

## Que se hizo
- [feature] Conexiones Google Drive por organización con secrets cifrados, OAuth state ligado a `org_id`, allowlist centralizada, containment de roots y rate limiting.
- [fix] `DrivePage` revoca la conexión seleccionada y remonta el navegador tras desconectar.
- [fix] La paginación conserva archivos visibles si falla una página adicional.
- [qa] QA15 queda desactivado por defecto y exige flag explícito más hostname exacto en `QA_DRIVE_ALLOWED_HOSTS`; producción queda rechazada.
- [docs] Actualizadas documentación, contrato API, variables de entorno y guía de QA.

## Verificacion
- `npm run typecheck` -> OK.
- `npm run lint` -> OK; solo warnings preexistentes.
- `npm run test:run` -> 106 archivos, 736 tests passed, 8 skipped.
- `npm run build` -> OK.
- `node --check scripts/qa-battery.js` y `scripts/qa-drive-guard.js` -> OK.
- `git diff --check` -> OK.
- Review independiente: sin findings tenant; los findings finales del guard fueron corregidos y cubiertos por tests.

## Pendientes del owner
- Aplicar las migraciones `057_drive_connections.sql` y `058_harden_drive_tenant_boundaries.sql` mediante el workflow controlado de Supabase, primero en staging y luego en producción con aprobación explícita.
- Configurar en el secret manager `GOOGLE_DRIVE_ROOT_FOLDER_IDS`, `DRIVE_TOKEN_ENCRYPTION_KEY` y el OAuth redirect URI; retirar `GOOGLE_DRIVE_FOLDER_ID` antiguo.
- Crear dos organizaciones/cuentas de prueba y ejecutar aislamiento tenant real en staging con las variables `DRIVE_TENANT_TEST_*` y `DRIVE_TENANT_TEST_ALLOW=I_UNDERSTAND_NON_PRODUCTION`.
- Ejecutar QA15 solo en un host permitido y no productivo, con usuario QA dedicado y `QA_DRIVE_CRUD_ALLOW=I_UNDERSTAND_NON_PRODUCTION`.

## Estado
- Commit y push realizados al cerrar esta sesión.
- No se ejecutaron migraciones, CRUD Drive destructivo ni validación contra producción.
