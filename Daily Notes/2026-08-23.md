---
fecha: "2026-08-23"
tipo: daily-note
tags: [diario, desarrollo]
---

# 2026-08-23 - QA Parte B/C: migración 039 + refactor getOrgId

## Lo que hice hoy

- Batería QA extendida a Suppliers/Sales/Inventory (`scripts/qa-battery.js`, `npm run qa:battery`): **24 PASS / 0 FAIL** en prod.
- Hallazgo #2 (crítico): `org_id` faltante en 8 tablas tenant-scoped → **migración 039 aplicada en prod** con aprobación del owner. Verificada end-to-end.
- Hallazgo #3 (crítico): 9 rutas leían `profiles.org_id` (columna inexistente) → refactor Opción B aprobado y aplicado: todas usan `getOrgId` estándar; `weekly-summary` itera `org_members`. Commit `0ddefc9`.
- Parte D: batería a **39 checks** (invalid data, orders/expenses/returns, probes post-refactor). Hallazgo #4: 11 rutas `[id]` sin guard UUID (500 con id malformado) → fix `isValidUuid` + tests ajustados. Commit `1ab4750`.
- Partes E/F/G: batería a **54 PASS / 0 FAIL**: Flow A purchase lifecycle completo, Flow B creación/consistencia parcial por falta de `/api/returns/[id]`, aislamiento multi-org API + UI verificado. Drive bloqueado por cuota de service account en producción; requiere Shared Drive u OAuth.
- Verificación completa: typecheck ✓ lint ✓ 425/425 tests ✓ build ✓ · **39/39 PASS en prod post-deploy**. Pusheado hasta `29bb27e`.

## Continuación — Share, Returns y Reembolsos

- Share deshabilitado completamente: `/api/share`, `/api/share/[token]` y `/share/[token]` bloqueados server-side; entrada Analytics oculta; código y datos conservados.
- Governance/Board Decisions archivado solo documentalmente en `docs/archive/GOVERNANCE.md`; runtime intacto.
- Implementado `/api/returns/[id]` para GET/PUT con UUID, org scope y role gate; POST ahora valida ownership del producto y editor+.
- Migración 051 aplicada y verificada en producción: tablas, 4 policies y RPC presentes. Auditoría de referencias tenant: 0 cruces en returns/reimbursements.
- Suite final actual: 81 archivos / 524 tests OK tras corregir guards y mocks; typecheck/build/E2E OK; lint solo warnings preexistentes.
- Drive OAuth endurecido con state CSRF, redirect URI centralizado y roles en mutaciones; queda pendiente configuración externa Google/Vercel y autorización de una cuenta.
- Migraciones 052-056 aplicadas en prod: RLS tenant para shipments/items/returns y relaciones legacy, roles, triggers y onboarding atómico; verificado 0/1 shipments y 0/0 items sin `org_id`.
- Pendientes: prueba RLS real A→B y E2E OAuth con cuenta del owner.

## Blockers

- Rutas SP-API refactorizadas solo verificables en vivo cuando owner conecte Seller Central real.

## Proximos pasos

- Verificación visual multi-org en UI + Drive upload E2E (requieren navegador del owner).
- Flow A/B lifecycle e invalid-data tests (pendientes manuales en `docs/QA_LOG.md`).
